{% extends 'base.html.twig' %}

{% block title %}{{ page_title }}{% endblock %}

{% block body %}
<div id="homepage-app">

    <!-- Hero Section -->
    {{ include('components/hero.html.twig') }}
    <!-- Features Section -->
    {{ include('components/features.html.twig') }}
    <!-- Testimonials Section -->
    {{ include('components/testimonials.html.twig') }}
    <!-- Pricing Section -->
    {{ include('components/pricing.html.twig') }}
    <!-- FAQ Section -->
    {{ include('components/faq.html.twig') }}
    <!-- Newsletter Section -->
    {{ include('components/newsletter.html.twig') }}

</div>
{% endblock %}
