{% extends 'base.html.twig' %}

{% block title %}{{ article.titre }} - Blog Stellara{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8 max-w-4xl">
    <nav class="mb-6">
        <a href="{{ path('blog_index') }}" class="text-blue-600 hover:text-blue-800">
            ← Retour au blog
        </a>
    </nav>
    
    <article class="bg-white rounded-lg shadow-lg p-8">
        <header class="mb-8">
            <h1 class="text-4xl font-bold mb-4">{{ article.titre }}</h1>
            
            <div class="text-gray-600 border-b pb-4">
                Par <strong>{{ article.auteur }}</strong> • 
                {{ article.createdAt|date('d/m/Y à H:i') }}
            </div>
        </header>
        
        <div class="prose prose-lg max-w-none">
            {{ article.contenu|raw }}
        </div>
        
        <footer class="mt-12 pt-8 border-t">
            <div class="bg-blue-50 p-6 rounded-lg">
                <h3 class="text-lg font-semibold mb-2">💡 Besoin d'estimer votre projet ?</h3>
                <p class="text-gray-700 mb-4">
                    Stellara vous aide à lire vos dynamiques internes et à préparer vos fenêtres d’opportunités personnelles.
                </p>
                <a href="{{ path('app_home') }}" 
                   class="inline-block bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors">
                    Découvrir mon tableau de bord →
                </a>
            </div>
        </footer>
    </article>
</div>

<style>
/* Styles spécifiques pour le contenu du blog */
.prose h2 {
    @apply text-2xl font-bold mt-8 mb-4 text-gray-800;
}

.prose h3 {
    @apply text-xl font-semibold mt-6 mb-3 text-gray-800;
}

.prose p {
    @apply mb-4 text-gray-700 leading-relaxed;
}

.prose ul {
    @apply mb-4 pl-6;
}

.prose ol {
    @apply mb-4 pl-6;
}

.prose li {
    @apply mb-2 text-gray-700;
}

.prose strong {
    @apply font-semibold text-gray-900;
}
</style>
{% endblock %}
