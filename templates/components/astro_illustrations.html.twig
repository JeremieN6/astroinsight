{# Decorative astro illustration banner: planets + constellations (accessible, non-blocking) #}
<section class="relative overflow-hidden bg-gradient-to-b from-indigo-50 to-white dark:from-gray-900 dark:to-gray-800">
  <div class="mx-auto max-w-screen-xl px-4 py-8 sm:py-10">
    <div class="relative w-full h-56 sm:h-72 md:h-80 rounded-2xl border border-indigo-100 dark:border-indigo-900/40 bg-white/60 dark:bg-indigo-900/20 backdrop-blur-sm">
      <svg viewBox="0 0 1200 400" width="100%" height="100%" class="absolute inset-0" role="img" aria-label="Illustration cosmique avec constellations et planètes légères">
        <defs>
          <radialGradient id="glow" cx="50%" cy="50%" r="50%">
            <stop offset="0%" stop-color="#a78bfa" stop-opacity="0.7" />
            <stop offset="100%" stop-color="#a78bfa" stop-opacity="0" />
          </radialGradient>
        </defs>
        <!-- Starfield -->
        {% set stars = [
          [50,60],[120,40],[180,100],[230,70],[290,30],[350,90],[420,50],[480,120],[540,80],[600,30],
          [660,110],[720,60],[780,90],[840,40],[900,100],[960,50],[1020,80],[1080,35],[1140,95]
        ] %}
        {% for s in stars %}
          <circle cx="{{ s[0] }}" cy="{{ s[1] }}" r="1.8" fill="#c7d2fe" opacity="0.9" />
        {% endfor %}

        <!-- Constellation lines -->
        <g stroke="#818cf8" stroke-width="1.5" opacity="0.7">
          <polyline points="160,220 220,180 300,210 360,170 430,200" fill="none" />
          <polyline points="700,260 760,220 820,250 900,210 980,240" fill="none" />
        </g>
        <!-- Constellation nodes -->
        <g fill="#a5b4fc">
          <circle cx="160" cy="220" r="4" />
          <circle cx="220" cy="180" r="4" />
          <circle cx="300" cy="210" r="4" />
          <circle cx="360" cy="170" r="4" />
          <circle cx="430" cy="200" r="4" />

          <circle cx="700" cy="260" r="4" />
          <circle cx="760" cy="220" r="4" />
          <circle cx="820" cy="250" r="4" />
          <circle cx="900" cy="210" r="4" />
          <circle cx="980" cy="240" r="4" />
        </g>

        <!-- Planet with glow -->
        <circle cx="1040" cy="120" r="60" fill="#6366f1" opacity="0.25" />
        <circle cx="1040" cy="120" r="68" fill="url(#glow)" />
        <ellipse cx="1040" cy="120" rx="95" ry="18" fill="#93c5fd" opacity="0.25" transform="rotate(-15 1040 120)" />

        <!-- Crescent moon -->
        <g transform="translate(140,110)">
          <circle cx="0" cy="0" r="30" fill="#fef3c7" />
          <circle cx="10" cy="-6" r="30" fill="#fff" />
        </g>
      </svg>
      <div class="absolute inset-0 pointer-events-none rounded-2xl ring-1 ring-inset ring-indigo-200/50 dark:ring-indigo-700/30"></div>
    </div>
    <p class="sr-only">Décor visuel cosmique illustrant l'univers astrologique d'AstroInsight.</p>
  </div>
  <div class="h-px bg-gradient-to-r from-transparent via-indigo-300/50 to-transparent dark:via-indigo-700/40"></div>
</section>
